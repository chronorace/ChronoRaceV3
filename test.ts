/**
 * Created by christian on 25/07/2014.
 */
module cr.test{
    class zen{
        constructor(private name?:string){
            if (!name) this.name="cle";
		alert('cool... c est la merde?');
            // test conflit depuis notepad
		// encore un ajout dans notepad a recuperer ds webstorm
        }
    }
}